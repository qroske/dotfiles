-- --------------------------------
-- alpha-nvim: ダッシュボード
-- --------------------------------
return {
  "goolord/alpha-nvim",
  dependencies = { "nvim-tree/nvim-web-devicons" },

  config = function()
    -- Alpha-nvimの機能を使えるようにする
    local alpha = require("alpha")
    local dashboard = require("alpha.themes.dashboard") -- ダッシュボード（スタート画面）のテーマを読み込む

    -- ===== ヘッダー部分（一番上に表示される大きな文字）=====
    dashboard.section.header.val = {
      "                                                     ",
      "  ███╗   ██╗███████╗ ██████╗ ██╗   ██╗██╗███╗   ██╗ ",
      "  ████╗  ██║██╔════╝██╔═══██╗██║   ██║██║████╗  ██║ ",
      "  ██╔██╗ ██║█████╗  ██║   ██║██║   ██║██║██╔██╗ ██║ ",
      "  ██║╚██╗██║██╔══╝  ██║   ██║╚██╗ ██╔╝██║██║╚██╗██║ ",
      "  ██║ ╚████║███████╗╚██████╔╝ ╚████╔╝ ██║██║ ╚████║ ",
      "  ╚═╝  ╚═══╝╚══════╝ ╚═════╝   ╚═══╝  ╚═╝╚═╝  ╚═══╝ ",
      "                                                     ",
    }

    -- ===== ボタン部分（押すと実行されるメニュー）=====
    dashboard.section.buttons.val = {
      -- 各ボタンの構造: button("押すキー", "表示される文字", "実行されるコマンド")

      -- fキーを押すと、ファイルを名前で検索できる
      dashboard.button("f", "  Find file", ":Telescope find_files <CR>"),
      -- pキーを押すと、プロジェクト（作業フォルダ）を切り替えられる
      dashboard.button("p", "  Find project", ":Telescope projects <CR>"),
      -- sキーを押すと、保存した作業状態（セッション）を探せる
      dashboard.button("s", "  Find session", ":Autosession search <CR>"),
      -- eキーを押すと、新しい空のファイルを作成して編集を開始
      dashboard.button("e", "  New file", ":ene <BAR> startinsert <CR>"),
      -- rキーを押すと、最近開いたファイルの一覧が表示される
      dashboard.button("r", "  Recently files", ":Telescope oldfiles <CR>"),
      -- tキーを押すと、ファイルの中身の文字を検索できる
      dashboard.button("t", "  Find text", ":Telescope live_grep <CR>"),
      -- nキーを押すと、ファイルツリー（フォルダ構造）を表示・非表示
      dashboard.button("n", "  File Explorer", ":Neotree toggle<CR>"),
      -- cキーを押すと、Neovimの設定ファイルを開ける
      dashboard.button("c", "  Configuration", ":e $MYVIMRC <CR>"),
      -- qキーを押すと、Neovimを終了する
      dashboard.button("q", "  Quit", ":qa<CR>"),
    }

    -- ===== フッター部分（一番下に表示される情報）=====
    -- 現在開いているプロジェクト（フォルダ）の名前を表示
    -- 例: 「🚀 Project: my-website」のように表示されます
    dashboard.section.footer.val = "🚀 Project: " .. vim.fn.fnamemodify(vim.fn.getcwd(), ":t")

    -- 上記の設定を適用してスタート画面を表示
    alpha.setup(dashboard.config)
  end,
}
